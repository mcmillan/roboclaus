<div class="devise-links">
  You can also

  <%=
    [
      controller_name != 'sessions' && link_to("sign in", new_session_path(resource_name), class: 'devise-links__link'),
      devise_mapping.registerable? && controller_name != 'registrations' && link_to("sign up", new_registration_path(resource_name), class: 'devise-links__link'),
      devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' && link_to( "reset your password", new_password_path(resource_name), class: 'devise-links__link'),
      devise_mapping.confirmable? && controller_name != 'confirmations' && link_to("re-request confirmation instructions", new_confirmation_path(resource_name), class: 'devise-links__link'),
      devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' &&link_to("re-request unlock instructions", new_unlock_path(resource_name), class: 'devise-links__link')
    ].reject(&:blank?).to_sentence(two_words_connector: ' or ').html_safe
  %>, if you'd rather.

  <%- if devise_mapping.omniauthable? %>
    <%- resource_class.omniauth_providers.each do |provider| %>
      <%= link_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider), class: 'devise-links__link' %>
    <% end %>
  <% end %>
</div>
