<% if resource.errors.any? %>
  <div class="alert alert--error">
    <strong>Oh no – something's not quite right:</strong>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
